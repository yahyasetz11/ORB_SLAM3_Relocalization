yolo_bbox_node:
  ros__parameters:
    device: 'cuda'    # 'cuda' or 'cpu' — node auto-falls back to cpu if CUDA unavailable
    display: true     # show YOLO detection window (set false to disable for navigation)
    conf: 0.2         # detection confidence threshold
